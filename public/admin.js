
document.addEventListener('DOMContentLoaded', () => {
    const registerButtons = document.querySelectorAll('.btn btn-register');
    registerButtons.forEach(button => {
      button.addEventListener('click', () => {
      });
    });
  
    const deactivateButtons = document.querySelectorAll('.btn btn-deactivate');
    deactivateButtons.forEach(button => {
      button.addEventListener('click', () => {
      });
    });
  

    const modifyButtons = document.querySelectorAll('.btn btn-modify');
    modifyButtons.forEach(button => {
      button.addEventListener('click', () => {
      });
    });
  
    const eliminateButtons = document.querySelectorAll('.btn btns-eliminate');
    eliminateButtons.forEach(button => {
      button.addEventListener('click', () => {

      });
    });
  
    // agregar más eventos según sea necesario para otros elementos de tu interfaz
  });
  